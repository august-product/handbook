# August Handbook

Onboarding tool for new and existing employees and management of August Collections.

## Setup

1. Install dependencies:

```bash
npm install
```

2. Run the dev server:

```bash
npm run dev
```

3. Open `http://localhost:3000`.

## Notes

- Auth uses the Xano endpoints provided in the app.
- Tokens are stored in `localStorage`.
